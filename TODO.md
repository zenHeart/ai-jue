# ai-jue 项目实施路线图 (TODO)

本文档基于“MVP 优先、配置自举、生态飞轮”的核心理念，对 `ai-jue` 项目的实施路径进行拆解。我们的目标是先用最小的代价验证核心闭环，然后快速利用工具自身实现经验沉淀，最终形成社区驱动的生态飞轮。

---

## Phase 0: MVP - 核心闭环与自我“自举”

**状态：** ✅ **已完成**

**目标：** 用最少的代码验证“通过预设生成AI配置文件”这一核心流程，并立即应用到 `ai-jue` 项目自身的开发中。

- [x] **基础 CLI 框架搭建**
    - [x] 使用 `yargs` 搭建基础命令，包含 `apply` 命令。
- [x] **配置文件读取**
    - [x] 使用 `cosmiconfig` 实现对配置文件的读取，并支持 `ai.*` 和 `jue.*` 的兼容查找。
- [x] **最简预设加载**
    - [x] 实现从 `node_modules` 中加载单个指定预设包的核心逻辑。
- [x] **插件化架构与文件生成**
    - [x] **[超前完成]** 架构已重构为“微内核 + 适配器插件”模式，替代了原计划的简单模板渲染。
    - [x] 文件生成逻辑已分离到 `ai-jue-adapter-*` 包中。
    - [x] **[关键要求]** 文件生成遵循“智能共存”策略：对 `.md` 文件采用“区块管理”，对 `.json` 文件采用“深度合并”。
- [x] **创建第一个核心预设 `jue-preset-base`**
    - [x] 已创建该预设包，并通过 `ai.config.js` 应用。
- [x] **[里程碑] 完成项目自我“自举”**
    - [x] 在 `ai-jue` 项目中成功安装并配置 `jue-preset-base` 及相关适配器。
    - [x] 成功运行 `npx jue apply`，生成了项目自身的 `GEMINI.md` 和 `CLAUDE.md`。
    - **第一个飞轮已经开始转动。**

---

## Phase 1: 可用性增强 & 本地资产管理

**状态：** ⏳ **进行中...** (Next Target)

**目标：** 让工具对单个用户足够好用，打通“使用预设”与“沉淀本地经验”的链路。

- [ ] **实现 `.ai` 目录作为本地资产工作区（并兼容 `.jue` 作为备选）**
    - [ ] **[当前任务]** 优化预设包的内部结构，采用“能力即目录”的规范，为加载本地资产做准备。
    - [ ] `apply` 命令需要能识别并加载此目录下的本地资产，并优先使用 `.ai`。
- [ ] **完善配置继承与组合能力**
    - [ ] 在 `ai.config.js` 中实现 `extends` 字段，用于合并本地 `.ai` 目录的资产。
    - [ ] 支持 `presets: ['base', 'react']` 数组，实现多个预设的组合。
    - [ ] **[关键要求]** 加载和合并资产时，必须支持“环境特定性”，即实现对专属子目录（如 `claude/`）的识别与“覆写/回退”逻辑。
- [ ] **实现 `init` 交互式命令**
    - [ ] 运行 `npx jue init` 可以引导用户创建 `ai.config.js` 基础配置和 `.ai` 目录结构。
- [ ] **支持多语言 (i18n) 资产**
    - [ ] 在 `ai.config.js` 中增加顶层的 `language` 选项。
    - [ ] 在资产加载逻辑中，增加对语言子目录（如 `zh-CN/`）的支持，实现“覆写/回退”机制。
- [ ] **完善辅助命令**
    - [ ] 实现 `npx jue list`，可以列出当前已加载的预设和资产。
    - [ ] 实现 `npx jue check` 命令，用于检测预设更新并提示用户。

---

## Phase 2: 生态构建 - 预设打包与分享

**状态：** ⏳ **未开始**

**目标：** 打通“沉淀”到“分享”的路径，驱动社区生态飞轮。

- [ ] **[核心功能] 设计并实现 `create-preset` 命令**
- [ ] **完善文档**
- [ ] **建立预设展示渠道**

---

## Phase 3: 架构进阶 & 工具链集成

**状态：** ⏳ **部分完成**

**目标：** 提供更高级的功能，并融入更广泛的开发工具链中。

- [x] **[超前完成] 编译器架构**: 当前的“微内核 + 适配器”模式已经是该思想的初步实现。
- [ ] **[高级功能] 实现软链接模式**
- [ ] **[效率工具] 开发 IDE 扩展 (VS Code)**
- [ ] **[生态集成] 探索与脚手架工具的集成**
- [ ] **[健壮性] 实现 `validate` 命令**

---

## Phase 4 / 远期战略思考

**状态：** ⏳ **部分完成**

**目标：** 解决项目在走向成熟和大规模应用时，必然会遇到的系统性、生态性问题。

- [x] **生态系统与治理**: 已在核心文档中明确了方向。
- [x] **开发者工作流的无缝集成**: 已在核心文档中明确了方向。
- [x] **跨工具抽象的挑战**: “编译器架构”和“逃生通道”的设计思想已在文档中确立。
- [x] **配置语言的表达力**: 已在核心文档中明确了方向。
