.specstory/**
.specstory/
.DS_Store
node_modules
dist/
cache

# AI-JUE generated files (repo-root only — do NOT ignore adapter fixtures
# under packages/*/fixtures/**/.claude or **/.agents)
/CLAUDE.md
/GEMINI.md
/.gemini/
# Cursor local state; keep shared agent rules tracked
/.cursor/*
!/.cursor/rules/
!/.cursor/rules/**
/.claude/
/.agents/
.turbo
**/.turbo/
**/.turbo/turbo-build.log

# local-only security blocklist (never commit real identifiers)
.security/blocklist.local.txt
