Neutral fixture project instructions for the Codex Adapter.

# Neutral Fixture Context

This is the shared, tool-agnostic project context for the Codex Adapter
fixture world. It exists only so `AGENTS.md` is the documented canonical
context file Codex actually reads (per JUE-104/105/JUE-301 discovery),
and the Adapter has somewhere real to round-trip a real value.

## Rule: style

Scope: src/**/*.ts

Use two spaces.
