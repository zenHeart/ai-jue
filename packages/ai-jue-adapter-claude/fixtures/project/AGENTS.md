# Neutral Fixture Context

This is the shared, tool-agnostic project context for the neutral fixture
world. It exists only so `CLAUDE.md` can prove it imports `AGENTS.md`
explicitly, since Claude Code reads `CLAUDE.md` and never reads `AGENTS.md`
on its own.
